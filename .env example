# Spotify API credentials
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
SPOTIFY_REDIRECT_URI=http://localhost:8080/callback
SPOTIFY_SCOPE=user-read-private,user-read-email,user-library-read,user-top-read # for this particular project

# PostgreSQL (Airflow metadata) configuration
POSTGRES_USER=airflow
POSTGRES_PASSWORD=your_secure_postgres_password
POSTGRES_DB=airflow
POSTGRES_PORT=5432

# Redis configuration
REDIS_PORT=6379

# Airflow configuration
AIRFLOW_WEBSERVER_PORT=8080
AIRFLOW_USERNAME=airflow
AIRFLOW_PASSWORD=your_secure_airflow_password
AIRFLOW_FERNET_KEY='' # Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_DAGS_PAUSED_AT_CREATION=true
AIRFLOW_LOAD_EXAMPLES=false

# Grafana configuration
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_grafana_password
GRAFANA_PLUGINS=grafana-clock-panel,grafana-simple-json-datasource

# Spotify database configuration
SPOTIFY_DB_USER=spotify
SPOTIFY_DB_PASSWORD=your_secure_spotify_db_password
SPOTIFY_DB_NAME=spotify_data
SPOTIFY_DB_PORT=5433

